import { LoginForm } from "@/app/components/auth/login-form";

export const metadata = {
  title: "Sign In | ApexFit",
  description: "Sign in to your ApexFit account.",
};

export default function LoginPage() {
  return <LoginForm />;
}